.DS_Store
*.swp
.vscode/
.idea/
.env
.venv/
__pycache__/

# telegram_state.json is intentionally tracked: the GitHub Action (.github/
# workflows/telegram-sync.yml) needs persisted state across runs, else CI would
# re-post every tea as a duplicate. It holds only message_ids/hashes — no secrets.
